# SpectralMol Python Sources

This directory contains the importable `core` and `benchmarks` packages. See
the repository-level `README.md` for installation, configuration, benchmark,
data, hardware, testing, and reproducibility instructions.

The supported public entry point is:

```bash
python run.py --help
```
